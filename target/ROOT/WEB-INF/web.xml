<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="http://xmlns.jcp.org/xml/ns/javaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee
                             http://xmlns.jcp.org/xml/ns/javaee/web-app_4_0.xsd"
         version="4.0">

    <display-name>Campus Hub IoT Management API</display-name>

    <!-- 
      Configure Jersey Servlet Container.
      We point it to our CampusHubApp class which extends ResourceConfig.
    -->
    <servlet>
        <servlet-name>CampusHubAPI</servlet-name>
        <servlet-class>org.glassfish.jersey.servlet.ServletContainer</servlet-class>
        <init-param>
            <param-name>javax.ws.rs.Application</param-name>
            <param-value>uk.ac.campus.boot.CampusHubApp</param-value>
        </init-param>
        <load-on-startup>1</load-on-startup>
    </servlet>

    <!-- 
      Map the Jersey servlet to /api/v1/* and let the servlet mapping define
      the API base path. This makes the API available at /api/v1/.
    -->
    <servlet-mapping>
        <servlet-name>CampusHubAPI</servlet-name>
        <url-pattern>/api/v1/*</url-pattern>
    </servlet-mapping>

</web-app>
